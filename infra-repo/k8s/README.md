Kubernetes manifests and installation helpers for the infra repo.

This folder contains higher-level instructions and can be used by the CI to store cluster bootstrap manifests.

See `infra-repo/envs/*` for environment-specific Terraform roots.
