apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: aws-secrets-manager
spec:
  provider:
    aws:
      service: SecretsManager
      region: "# TODO: ADD_REGION"
      auth:
        jwt:
          serviceAccountRef:
            name: external-secrets-sa
            namespace: external-secrets

# TODO: USER_ACTION_REQUIRED - Create an IRSA role for the External Secrets service account or configure AWS credentials for the operator.
