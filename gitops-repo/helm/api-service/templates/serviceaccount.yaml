apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ default (include "api-service.fullname" .) .Values.serviceAccount.name | quote }}
  labels:
    app: {{ include "api-service.name" . }}
  annotations:
{{ toYaml .Values.serviceAccount.annotations | indent 4 }}
